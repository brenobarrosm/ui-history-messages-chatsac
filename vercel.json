{
  "version": 2,
  "builds": [
    {
      "src": "server.py",
      "use": "@vercel/python"
    }
  ],
  "env": {
    "ACCESS_TOKEN": "@access_token"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server.py"
    }
  ]
}
